messages:
  prefix: "&8[&dUTR&8] &r"
  not_player: "&c플레이어만 사용 가능합니다."
  rank_info:
    now: "&7현재 등급: {now}"
    next: "&7다음 등급까지 {need_minutes}분 → {next} (&8{group}&7)"
title_suffix:
  enabled: true
  priority: 100
  format: "&7[&f{title}&7]"
  clear_command: "lp user {player} meta removesuffix {priority}"
  set_command:   "lp user {player} meta setsuffix {priority} {suffix}"
auto_promote:
  enabled: true
  add_command: "lp user {player} parent add {group}"
  remove:
    enabled: false
    remove_previous_rank_group: true
    groups_extra: []
    remove_command: "lp user {player} parent remove {group}"
title_voucher:
  name: "&d칭호 커스텀권"
  lore:
    - "&7우클릭 후 채팅에 칭호를 입력하세요"
    - "&7색코드(&) 지원"
colorchat:
  colors:
    - { id: "red", name: "빨강", code: "&c", material: "RED_DYE" }
    - { id: "blue", name: "파랑", code: "&9", material: "LAPIS_LAZULI" }
ranks:
  - id: newbie
    display: "&7[&f새싹&7]"
    required_minutes: 0
    luckperms_group: "rank_newbie"
    view_limits: { legendary_per_day: 1, megaboss_per_day: 1 }
  - id: regular
    display: "&a[레귤러]"
    required_minutes: 180
    luckperms_group: "rank_regular"
    view_limits: { legendary_per_day: 3, megaboss_per_day: 3 }
